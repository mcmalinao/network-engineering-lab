# containerlab

Containerlab topology definitions used to stand up virtual network labs for
validating designs before (or instead of) physical hardware.

**Status:** Scaffolded, no content yet — start with `/speckit-specify`.
