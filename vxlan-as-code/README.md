# vxlan-as-code

VXLAN fabric definitions expressed as code — underlay/overlay parameters
and generated configuration, independent of a specific vendor controller.

**Status:** Scaffolded, no content yet — start with `/speckit-specify`.
