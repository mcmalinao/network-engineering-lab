# specs

Spec Kit feature specs, implementation plans, and task lists generated by
`/speckit-specify`, `/speckit-plan`, and `/speckit-tasks`. Each feature gets
its own subdirectory here.

**Status:** Scaffolded, no content yet.
